---
import "../styles/CardHeader.scss";

const {tier} = Astro.props as {tier: string};
const tier_class = tier.toLowerCase().replace(" ", "-");
---

<div class={`card__header --${tier_class}`}>
	<p class='card__tier'>{tier}</p>
</div>

<style lang='scss'></style>
